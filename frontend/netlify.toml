# Netlify build config (set "Base directory" to "frontend" in Netlify UI)
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so direct links and refresh work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
